const struct = [

]

// object maybe just fine -- jsu tlooping trough instances is a lot slower

// const create = (target, val, stamp, parent, noInstances, key) => {

// ok soe lets try object less options as well which is ncie


// parent, key, noInstances, stamp

module.exports = (target, val) => {
  // do it -- no target means default
}
